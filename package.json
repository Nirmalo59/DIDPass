{
  "name": "didpass",
  "version": "1.0.0",
  "description": "DIDPass – Decentralized Identity Wallet. Blockchain-powered SSO using verifiable credentials and MetaMask.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy:demo": "hardhat run scripts/setup-demo.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2"
  }
}
